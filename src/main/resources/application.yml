spring:
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8081

cors:
  allowUrl: "*"
jwt:
  secret: secret_code
  expirationTime: 30h